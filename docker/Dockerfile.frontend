# 前端生产镜像：使用已完成构建的静态产物，服务器无需安装 Node/npm 依赖
FROM nginx:alpine

# 复制构建产物
COPY frontend/dist /usr/share/nginx/html

# 复制 nginx 配置
COPY docker/nginx.conf /etc/nginx/conf.d/default.conf

# 暴露端口
EXPOSE 80

# 启动 nginx
CMD ["nginx", "-g", "daemon off;"]
